# RocketMQ Broker —— Slave 节点配置素材
# 与 broker-master.conf 差异：brokerId=1 + brokerRole=SLAVE（同为 {{broker_name}} 组，作为 Master 的从库）。
# namesrvAddr 填集群全部 namesrv 的 IP:9876；brokerIP1 务必为宿主机真实内网 IP。

brokerClusterName={{cluster_name}}
brokerName={{broker_name}}
# 同组 Slave：brokerId 取非 0 正整数，与同 brokerName 的 Master(brokerId=0) 组成主备；
# brokerRole 必须显式配 SLAVE——实测（4.9.7）：只配 brokerId=1 而不配 brokerRole 时，
# broker 仍以 brokerId=0（master）启动，与真正的 master 每 30 秒互相覆盖注册，
# clusterList 只剩一行、无任何 HA 能力。
brokerId={{__broker_id}}
brokerRole=SLAVE

namesrvAddr={{namesrv_addr}}
brokerIP1={{__ip}}

listenPort={{broker_port}}
autoCreateTopicEnable=true

storePathRootDir=/home/rocketmq/store
storePathCommitLog=/home/rocketmq/store/commitlog

flushDiskType=ASYNC_FLUSH